[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm && pnpm install --frozen-lockfile && pnpm --filter @workspace/aura2-website run build"

[deploy]
startCommand = "npx serve -s artifacts/aura2-website/dist/public -l $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
