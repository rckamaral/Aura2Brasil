#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../orval/dist/bin/orval.mjs" "$@"
else
  exec node  "$basedir/../orval/dist/bin/orval.mjs" "$@"
fi
# cmd-shim-target=C:/Users/erick/Downloads/NOVO SITE/Game-UI-Refactor/Game-UI-Refactor/node_modules/orval/dist/bin/orval.mjs
